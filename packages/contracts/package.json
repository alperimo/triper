{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Arcium MPC smart contracts for Triper",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy": "anchor deploy",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "format": "cargo fmt --all"
  },
  "devDependencies": {
    "@arcium-hq/client": "^0.3.0",
    "@coral-xyz/anchor": "^0.30.1",
    "@lightprotocol/compressed-token": "0.14.0",
    "@lightprotocol/stateless.js": "0.14.0",
    "@solana/web3.js": "^1.98.4",
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "chai": "^6.2.0",
    "mocha": "^11.7.4",
    "ts-mocha": "^11.1.0"
  }
}
